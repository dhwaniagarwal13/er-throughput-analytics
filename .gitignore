.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.ipynb_checkpoints/

# Data artefacts — regenerable, never committed.
# The Kaggle source CSV is deliberately excluded: it is not ours to
# redistribute. `make ingest` fetches it (see src/erops/ingest.py).
data/raw/*
data/exports/*
!data/raw/.gitkeep
!data/exports/.gitkeep
*.duckdb
*.duckdb.wal

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml
logs/

# BI
*.pbix.lock
~$*

.env
.DS_Store

# Local agent tooling state
.claude-flow/
